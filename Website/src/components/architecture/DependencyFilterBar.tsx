import type { ModuleCategory } from "@/types";
export type ArchitectureFilter = "all" | Exclude<ModuleCategory,"data">;
const options: Array<{id:ArchitectureFilter;label:string}> = [{id:"all",label:"All"},{id:"runtime",label:"Runtime"},{id:"retrieval",label:"Retrieval"},{id:"graph",label:"Graph"},{id:"utilities",label:"Utilities"},{id:"external",label:"External Services"}];
export function DependencyFilterBar({value,onChange}:{value:ArchitectureFilter;onChange:(value:ArchitectureFilter)=>void}) { return <div className="flex flex-wrap items-center gap-2 rounded-lg border border-slate-200 bg-white p-3"><span className="mr-2 text-xs font-semibold uppercase tracking-wide text-slate-500">Show dependencies</span>{options.map(option=><button key={option.id} type="button" onClick={()=>onChange(option.id)} aria-pressed={value===option.id} className={`rounded-md border px-3 py-1.5 text-sm font-medium transition ${value===option.id?"border-cyan-600 bg-cyan-50 text-cyan-900":"border-slate-200 bg-white text-slate-600 hover:bg-slate-50"}`}>{option.label}</button>)}</div> }
