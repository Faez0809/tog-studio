import { Boxes, Braces, Cloud, Network } from "lucide-react"; import type { ArchitecturePageData } from "@/types";
export function ModuleStatsBar({data}:{data:ArchitecturePageData}){const values=[[Boxes,data.files.length,'Modules'],[Network,data.moduleEdges.length,'Connections'],[Braces,data.files.reduce((n,f)=>n+f.majorFunctions.length,0),'Major symbols'],[Cloud,data.externalServices.length,'External services']] as const;return <div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{values.map(([Icon,value,label])=><div key={label} className="flex items-center gap-3 rounded-lg border border-slate-200 bg-white p-3"><span className="rounded-md bg-slate-100 p-2"><Icon className="h-4 w-4 text-slate-600"/></span><div><p className="text-lg font-semibold text-slate-950">{value}</p><p className="text-xs text-slate-500">{label}</p></div></div>)}</div>}
