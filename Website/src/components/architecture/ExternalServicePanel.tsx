import { AlertTriangle, Cloud } from "lucide-react"; import type { ArchitecturePageData } from "@/types";
export function ExternalServicePanel({services}:{services:ArchitecturePageData['externalServices']}){return <section><div className="mb-4"><p className="text-xs font-semibold uppercase tracking-wide text-rose-700">System boundary</p><h2 className="mt-1 text-lg font-semibold text-slate-950">External services and failure points</h2></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{services.map(s=><article key={s.name} className="rounded-lg border border-slate-200 bg-white p-4"><div className="flex items-center gap-2"><span className="rounded bg-rose-50 p-2"><Cloud className="h-4 w-4 text-rose-600"/></span><h3 className="text-sm font-semibold text-slate-950">{s.name}</h3></div><p className="mt-3 text-xs leading-5 text-slate-600">{s.purpose}</p><p className="mt-3 text-[10px] font-bold uppercase text-slate-400">Used by</p><p className="mt-1 text-xs font-medium text-cyan-800">{s.usedBy.join(', ')}</p><div className="mt-3 border-t border-slate-100 pt-3"><p className="flex items-center gap-1 text-[10px] font-bold uppercase text-amber-700"><AlertTriangle className="h-3 w-3"/>Failure cases</p><ul className="mt-1 space-y-1">{s.failureModes.map(f=><li key={f} className="text-[11px] leading-4 text-slate-600">• {f}</li>)}</ul></div></article>)}</div></section>}
