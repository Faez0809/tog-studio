import { ArrowRight } from "lucide-react"; import { dataFlowStages } from "@/data";
export function DataFlowDiagram({selected,onSelect}:{selected:string;onSelect:(id:string)=>void}){const stage=dataFlowStages.find(s=>s.id===selected)??dataFlowStages[0];return <section className="rounded-lg border border-slate-200 bg-white p-5"><div><p className="text-xs font-semibold uppercase tracking-wide text-cyan-700">Runtime story</p><h2 className="mt-1 text-lg font-semibold text-slate-950">How data becomes an answer</h2></div><div className="mt-4 flex items-stretch overflow-x-auto pb-2">{dataFlowStages.map((s,i)=><div key={s.id} className="flex shrink-0 items-center"><button type="button" onClick={()=>onSelect(s.id)} className={`h-full w-36 rounded-md border p-3 text-left transition ${selected===s.id?"border-cyan-600 bg-cyan-50 ring-2 ring-cyan-100":"border-slate-200 bg-slate-50 hover:border-slate-300"}`}><span className="text-[10px] font-bold text-slate-400">{String(i+1).padStart(2,'0')}</span><p className="mt-1 text-xs font-semibold text-slate-900">{s.name}</p></button>{i<dataFlowStages.length-1?<ArrowRight className="mx-2 h-4 w-4 text-slate-300"/>:null}</div>)}</div><div className="mt-3 rounded-md border border-slate-200 bg-slate-50 p-4"><p className="text-sm font-medium text-slate-900">{stage.name}</p><p className="mt-1 text-sm leading-6 text-slate-600">{stage.detail}</p><div className="mt-2 flex flex-wrap gap-1.5">{stage.modules.map(m=><span key={m} className="rounded bg-white px-2 py-1 font-mono text-xs text-cyan-800">{m}</span>)}</div></div></section>}
