import { MousePointer2 } from "lucide-react";
import type { GraphExample } from "@/types";
import { EdgeInspector } from "./EdgeInspector";import { NodeInspector } from "./NodeInspector";
export function GraphInspector({example,depth,nodeId,edgeId}:{example:GraphExample;depth:number;nodeId:string|null;edgeId:string|null}){const node=example.nodes.find(n=>n.id===nodeId),edge=example.edges.find(e=>e.id===edgeId);return <aside className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><div className="mb-4 border-b border-slate-100 pb-3"><h2 className="text-sm font-bold text-slate-900">Graph inspector</h2><p className="text-xs text-slate-500">Entity and relation details</p></div>{node?<NodeInspector node={node} example={example} depth={depth}/>:edge?<EdgeInspector edge={edge} example={example}/>:<div className="grid min-h-56 place-items-center text-center"><div><MousePointer2 className="mx-auto text-slate-300" size={30}/><p className="mt-3 text-sm font-semibold text-slate-600">Select a graph element</p><p className="mt-1 text-xs leading-5 text-slate-400">Click any node or edge to inspect its role in TOG.</p></div></div>}</aside>}
