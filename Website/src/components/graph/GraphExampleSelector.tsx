import { Clapperboard, Landmark, Ship, Zap } from "lucide-react";
import type { GraphExample } from "@/types";
const icons=[Landmark,Clapperboard,Zap,Ship];
export function GraphExampleSelector({examples,selectedId,onSelect}:{examples:GraphExample[];selectedId:string;onSelect:(x:GraphExample)=>void}) { return <section><div className="mb-2 flex items-end justify-between"><div><h1 className="text-xl font-bold text-slate-950">Graph Playground</h1><p className="text-xs text-slate-500">Choose a static TOG reasoning scenario</p></div><span className="rounded-full bg-violet-100 px-3 py-1 text-[10px] font-bold uppercase tracking-wider text-violet-700">Educational sandbox</span></div><div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-4">{examples.map((x,i)=>{const Icon=icons[i];const active=x.id===selectedId;return <button key={x.id} onClick={()=>onSelect(x)} className={`flex items-center gap-3 rounded-xl border p-3 text-left transition ${active?"border-violet-400 bg-violet-50 ring-1 ring-violet-200":"border-slate-200 bg-white hover:border-violet-200 hover:bg-slate-50"}`}><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-lg ${active?"bg-violet-600 text-white":"bg-slate-100 text-slate-500"}`}><Icon size={18}/></span><span><b className="block text-sm text-slate-900">{x.title}</b><span className="text-[11px] text-slate-500">{x.subtitle}</span></span></button>})}</div></section> }
