import { ArrowRight, CheckCircle2, HelpCircle } from "lucide-react";
import type { GraphExample } from "@/types";
export function ReasoningPathViewer({example,depth}:{example:GraphExample;depth:number}){const items=example.reasoningPath.filter(id=>example.nodes.find(n=>n.id===id)?.depth!<=depth||example.edges.find(e=>e.id===id)?.depth!<=depth);return <section className="rounded-xl border border-slate-200 bg-white p-4"><div className="grid gap-4 lg:grid-cols-[1fr_auto]"><div><p className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-wider text-slate-400"><HelpCircle size={13}/>Question</p><h2 className="mt-1 text-base font-bold text-slate-900">{example.question}</h2><div className="mt-3 flex flex-wrap items-center gap-2">{items.map((id,i)=>{const n=example.nodes.find(x=>x.id===id),e=example.edges.find(x=>x.id===id);return <span className="contents" key={id}>{e?<span className="rounded-full bg-amber-100 px-2.5 py-1 font-mono text-[10px] font-bold text-amber-800">{e.relation}</span>:<span className={`rounded-md border px-2.5 py-1 text-xs font-semibold ${n?.type==="answer"?"border-emerald-300 bg-emerald-50 text-emerald-800":"border-slate-200 bg-slate-50"}`}>{n?.label}</span>}{i<items.length-1&&<ArrowRight size={13} className="text-slate-300"/>}</span>})}</div></div><div className={`min-w-48 rounded-lg border p-3 ${depth===example.maxDepth?"border-emerald-200 bg-emerald-50":"border-slate-200 bg-slate-50"}`}><p className="flex items-center gap-1.5 text-[10px] font-bold uppercase tracking-wider text-slate-400"><CheckCircle2 size={13}/>Final answer</p><p className="mt-1 text-base font-bold text-slate-900">{depth===example.maxDepth?example.answer:"Keep expanding…"}</p></div></div></section>}
