import { CircleDot, GitBranch, Network, Route, Waypoints } from "lucide-react";
import type { GraphExample } from "@/types";
export function GraphMetricsPanel({example,depth}:{example:GraphExample;depth:number}){const nodes=example.nodes.filter(n=>n.depth<=depth),edges=example.edges.filter(e=>e.depth<=depth),avg=nodes.length?(edges.length*2/nodes.length).toFixed(1):"0";const metrics=[{I:CircleDot,l:"Nodes",v:nodes.length},{I:GitBranch,l:"Edges",v:edges.length},{I:Waypoints,l:"Depth",v:`${depth}/${example.maxDepth}`},{I:Network,l:"Avg. degree",v:avg},{I:Route,l:"Multi-hop",v:Math.max(0,depth-1)}];return <div className="grid grid-cols-2 gap-2 sm:grid-cols-5">{metrics.map(({I,l,v})=><div className="rounded-lg border border-slate-200 bg-white px-3 py-2" key={l}><p className="flex items-center gap-1.5 text-[9px] font-bold uppercase tracking-wider text-slate-400"><I size={12}/>{l}</p><p className="mt-0.5 text-lg font-bold text-slate-900">{v}</p></div>)}</div>}
