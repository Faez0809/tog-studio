import { ArrowRight, Lightbulb } from "lucide-react";
import type { GraphExample, PlaygroundEdge } from "@/types";
export function EdgeInspector({edge,example}:{edge:PlaygroundEdge;example:GraphExample}){const source=example.nodes.find(n=>n.id===edge.source)!,target=example.nodes.find(n=>n.id===edge.target)!;return <div className="space-y-4"><div><p className="text-[10px] font-bold uppercase tracking-wider text-amber-600">Selected relation</p><h3 className="mt-1 font-mono text-lg font-bold text-slate-950">{edge.relation}</h3></div><div className="flex items-center gap-2 rounded-lg bg-slate-50 p-3 text-xs font-semibold"><span className="rounded bg-white px-2 py-1 shadow-sm">{source.label}</span><ArrowRight size={15} className="text-slate-400"/><span className="rounded bg-white px-2 py-1 shadow-sm">{target.label}</span></div><div className="rounded-lg border border-amber-100 bg-amber-50 p-3"><p className="flex items-center gap-1.5 text-xs font-bold text-amber-800"><Lightbulb size={14}/>Why this relation matters</p><p className="mt-1 text-xs leading-5 text-amber-700">{edge.whyItMatters}</p></div><p className={`rounded-full px-3 py-1.5 text-center text-[10px] font-bold uppercase tracking-wider ${edge.inReasoningPath?"bg-emerald-100 text-emerald-700":"bg-slate-100 text-slate-500"}`}>{edge.inReasoningPath?"Used in reasoning path":"Explored alternative"}</p></div>}
