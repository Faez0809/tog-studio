import { Focus, Lock, MousePointer2 } from "lucide-react";
export function GraphControls({pathOnly,onPathOnly,onFit}:{pathOnly:boolean;onPathOnly:()=>void;onFit:()=>void}) { const btn="inline-flex items-center gap-2 rounded-md border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-600 shadow-sm hover:border-violet-300 hover:text-violet-700";return <div className="flex flex-wrap gap-2"><button className={btn} onClick={onFit}><Focus size={14}/> Fit graph</button><button className={`${btn} ${pathOnly?"border-amber-300 bg-amber-50 text-amber-800":""}`} onClick={onPathOnly}>{pathOnly?<Lock size={14}/>:<MousePointer2 size={14}/>} {pathOnly?"Reasoning path only":"Show reasoning path"}</button></div> }
