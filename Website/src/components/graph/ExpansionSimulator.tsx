import { ChevronRight, Pause, Play, RotateCcw, Shrink } from "lucide-react";
export function ExpansionSimulator({depth,maxDepth,replaying,onExpand,onCollapse,onReset,onReplay}:{depth:number;maxDepth:number;replaying:boolean;onExpand:()=>void;onCollapse:()=>void;onReset:()=>void;onReplay:()=>void}){const btn="inline-flex items-center gap-2 rounded-md border border-slate-200 bg-white px-3 py-2 text-xs font-semibold text-slate-700 shadow-sm transition hover:border-cyan-300 hover:text-cyan-700 disabled:opacity-40";return <div className="flex flex-wrap items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 p-3"><div className="mr-2"><p className="text-xs font-bold text-slate-900">Expansion simulator</p><p className="text-[10px] text-slate-500">Frame {depth+1} of {maxDepth+1}</p></div><button className={`${btn} border-cyan-600 bg-cyan-600 text-white hover:bg-cyan-700 hover:text-white`} disabled={depth>=maxDepth||replaying} onClick={onExpand}><ChevronRight size={15}/>Expand one hop</button><button className={btn} onClick={onCollapse}><Shrink size={15}/>Collapse graph</button><button className={btn} onClick={onReset}><RotateCcw size={15}/>Reset</button><button className={btn} onClick={onReplay}>{replaying?<Pause size={15}/>:<Play size={15}/>} {replaying?"Pause replay":"Replay expansion"}</button><div className="ml-auto flex gap-1">{Array.from({length:maxDepth+1},(_,i)=><i key={i} className={`h-2 w-8 rounded-full transition ${i<=depth?"bg-cyan-500":"bg-slate-200"}`}/>)}</div></div>}
