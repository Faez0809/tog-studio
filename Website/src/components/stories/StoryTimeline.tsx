import { Check, Circle } from "lucide-react";import type { RuntimeStory } from "@/data/runtimeStories";
export function StoryTimeline({story,current,onChange}:{story:RuntimeStory;current:number;onChange:(n:number)=>void}){return <div className="surface h-full p-4"><p className="eyebrow mb-4">Execution timeline</p><div className="max-h-[420px] space-y-1 overflow-y-auto pr-1">{story.executionSteps.map((s,i)=><button key={i} onClick={()=>onChange(i)} className={`flex w-full gap-3 rounded-xl p-3 text-left transition ${i===current?"bg-cyan-950 text-white":i<current?"bg-cyan-50":"hover:bg-slate-100"}`}><span className={`mt-0.5 grid h-5 w-5 flex-none place-items-center rounded-full border ${i<=current?"border-cyan-400 bg-cyan-500 text-white":"border-slate-300"}`}>{i<current?<Check className="h-3 w-3"/>:<Circle className="h-2 w-2"/>}</span><span><strong className="block text-xs">{s.title}</strong><small className={`mt-1 block ${i===current?"text-cyan-100":"text-slate-500"}`}>{s.phase}{s.depth?` · Depth ${s.depth}`:""}</small></span></button>)}</div></div>}
