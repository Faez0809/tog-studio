import { motion } from "framer-motion"; import type { AnimationState } from "./VisualizationControls";
export function RelationPruningAnimation({playing,speed,replayKey}:AnimationState){const data=[["founder",.96],["birth_place",.89],["occupation",.81],["spouse",.31],["country",.22],["educated_at",.18],["award",.12]] as const;return <div key={replayKey} className="space-y-2">{data.map(([name,score],i)=><motion.div key={name} initial={{opacity:1,x:0}} animate={playing&&score<.5?{opacity:.18,x:16}:{opacity:1,x:0}} transition={{delay:(i*.16+.5)/speed}} className="flex min-w-0 items-center gap-3 rounded-lg border border-slate-200 p-2.5"><span className="min-w-0 flex-1 break-words text-xs font-semibold">{name}</span><div className="h-1.5 w-20 max-w-[30%] overflow-hidden rounded bg-slate-100"><motion.div initial={{width:0}} animate={playing?{width:`${score*100}%`}:{}} transition={{duration:.7/speed}} className="h-full bg-cyan-500"/></div><span className="w-9 text-right font-mono text-xs">{score}</span></motion.div>)}</div>}
