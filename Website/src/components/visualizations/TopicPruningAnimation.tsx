import { motion } from "framer-motion"; import type { AnimationState } from "./VisualizationControls";
export function TopicPruningAnimation({playing,speed,replayKey}:AnimationState){return <div key={replayKey} className="flex min-h-40 flex-wrap content-center justify-center gap-2">{Array.from({length:20},(_,i)=><motion.span key={i} initial={{opacity:1,scale:1}} animate={playing&&i>2?{opacity:.12,scale:.72}:{opacity:1,scale:1}} transition={{delay:(i%6)*.12/speed,duration:.5/speed}} className={`max-w-full break-words rounded-full border px-3 py-1.5 text-xs font-semibold ${i<3?"border-emerald-300 bg-emerald-50 text-emerald-800":"border-slate-200 bg-slate-50 text-slate-600"}`}>{["Microsoft","Bill Gates","Seattle"][i]??`Candidate ${i+1}`}</motion.span>)}</div>}
