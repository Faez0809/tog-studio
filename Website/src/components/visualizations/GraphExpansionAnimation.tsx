import { motion } from "framer-motion"; import { ArrowDown } from "lucide-react"; import type { AnimationState } from "./VisualizationControls";
export function GraphExpansionAnimation({playing,speed,replayKey}:AnimationState){return <div key={replayKey} className="flex min-h-64 flex-col items-center justify-center">{["Microsoft","Bill Gates","Seattle"].map((n,i)=><div key={n} className="contents"><motion.div initial={{opacity:0,scale:.7}} animate={playing?{opacity:1,scale:1}:{}} transition={{delay:i*.65/speed,type:"spring"}} className="max-w-full break-words rounded-xl border border-cyan-300 bg-cyan-50 px-6 py-3 text-center text-sm font-bold text-cyan-900">{n}</motion.div>{i<2&&<motion.div initial={{opacity:0}} animate={playing?{opacity:1}:{}} transition={{delay:(i*.65+.4)/speed}}><ArrowDown className="my-2 text-slate-400"/></motion.div>}</div>)}</div>}
