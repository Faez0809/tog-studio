import { motion } from "framer-motion"; import type { AnimationState } from "./VisualizationControls";
export function EvidenceRankingAnimation({playing,speed,replayKey}:AnimationState){const rows=[["Bill Gates co-founded Microsoft in 1975.",.97],["Microsoft is headquartered in Redmond.",.84],["Seattle is a city in Washington.",.62],["The company develops software.",.41]] as const;return <div key={replayKey} className="space-y-2">{rows.map(([text,score],i)=><motion.div key={text} initial={{opacity:0,y:14}} animate={playing?{opacity:1,y:0}:{}} transition={{delay:i*.2/speed}} className={`flex min-w-0 gap-3 rounded-lg border p-3 ${i===0?"border-amber-300 bg-amber-50":"border-slate-200 bg-white"}`}><span className="min-w-0 flex-1 break-words text-xs leading-5">{text}</span><motion.span initial={{opacity:0}} animate={playing?{opacity:1}:{}} transition={{delay:(i*.2+.45)/speed}} className="shrink-0 font-mono text-xs font-bold">{score}</motion.span></motion.div>)}</div>}
