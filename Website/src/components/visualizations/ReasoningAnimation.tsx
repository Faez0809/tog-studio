import { motion } from "framer-motion"; import { ArrowDown } from "lucide-react"; import type { AnimationState } from "./VisualizationControls";
export function ReasoningAnimation({playing,speed,replayKey}:AnimationState){return <div key={replayKey} className="flex flex-col items-center">{["Question","Evidence","Graph path","Reasoning","Answer"].map((x,i)=><div key={x} className="contents"><motion.div initial={{opacity:0,x:-12}} animate={playing?{opacity:1,x:0}:{}} transition={{delay:i*.35/speed}} className={`w-full max-w-sm rounded-lg border p-2.5 text-center text-xs font-bold ${i===4?"border-emerald-300 bg-emerald-50":"border-slate-200 bg-white"}`}>{x}</motion.div>{i<4&&<ArrowDown size={17} className="my-1 text-slate-400"/>}</div>)}</div>}
