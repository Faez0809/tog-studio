import { Braces,FileCode2,Layers3,Waypoints } from "lucide-react";import type { FunctionSpec } from "@/types";
export function FunctionStatsBar({items,visibleCount}:{items:FunctionSpec[];visibleCount:number}){const stats=[[Braces,visibleCount,"Functions shown"],[FileCode2,new Set(items.map(x=>x.file)).size,"Source files"],[Layers3,new Set(items.map(x=>x.stage)).size,"Pipeline stages"],[Waypoints,items.reduce((n,x)=>n+x.calls.length,0),"Documented calls"]] as const;return <div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{stats.map(([Icon,n,label])=><div key={label} className="flex items-center gap-3 rounded-lg border border-slate-200 bg-white p-3"><span className="rounded-md bg-slate-100 p-2"><Icon className="h-4 w-4 text-slate-600"/></span><div><p className="text-lg font-bold text-slate-950">{n}</p><p className="text-xs text-slate-500">{label}</p></div></div>)}</div>}
