import { ArrowDownLeft,ArrowUpRight } from "lucide-react";import type { FunctionSpec } from "@/types";
const Group=({title,items,Icon,onSelect}:{title:string;items:string[];Icon:typeof ArrowUpRight;onSelect:(name:string)=>void})=><div><h4 className="flex items-center gap-1.5 text-xs font-bold uppercase tracking-wide text-slate-500"><Icon className="h-3.5 w-3.5"/>{title}</h4><div className="mt-2 flex flex-wrap gap-1.5">{items.length?items.map(x=><button key={x} onClick={()=>onSelect(x)} className="rounded border border-slate-200 bg-slate-50 px-2 py-1 font-mono text-[11px] text-slate-700 hover:border-cyan-300 hover:bg-cyan-50">{x}</button>):<span className="text-xs italic text-slate-400">None in the documented chain</span>}</div></div>;
export function FunctionRelationshipPanel({fn,onSelect}:{fn:FunctionSpec;onSelect:(name:string)=>void}){return <section className="grid gap-4 rounded-lg border border-slate-200 bg-slate-50 p-3 sm:grid-cols-2 xl:grid-cols-1"><Group title="Calls" items={fn.calls} Icon={ArrowUpRight} onSelect={onSelect}/><Group title="Called by" items={fn.calledBy} Icon={ArrowDownLeft} onSelect={onSelect}/></section>}
