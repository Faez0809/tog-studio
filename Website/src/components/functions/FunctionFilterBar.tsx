import type { FunctionCategory } from "@/types";import { functionStages } from "@/data/functions";
export type FunctionFilters={category:"All"|FunctionCategory;file:string;stage:string;visualization:string};
export function FunctionFilterBar({value,onChange,files,visualizations}:{value:FunctionFilters;onChange:(v:FunctionFilters)=>void;files:string[];visualizations:string[]}){const cats:FunctionFilters["category"][]=["All","Retrieval","Graph","Ranking","Reasoning","Utilities"];const select="h-9 rounded-md border border-slate-300 bg-white px-2 text-xs text-slate-700 outline-none focus:border-cyan-500";return <div className="space-y-3 rounded-lg border border-slate-200 bg-white p-3"><div className="flex flex-wrap gap-1.5">{cats.map(category=><button key={category} onClick={()=>onChange({...value,category})} className={`rounded-md px-3 py-1.5 text-xs font-semibold transition ${value.category===category?"bg-slate-900 text-white":"bg-slate-100 text-slate-600 hover:bg-slate-200"}`}>{category}</button>)}</div><div className="flex flex-wrap gap-2"><select aria-label="Filter by file" value={value.file} onChange={e=>onChange({...value,file:e.target.value})} className={select}><option value="">All files</option>{files.map(x=><option key={x}>{x}</option>)}</select><select aria-label="Filter by stage" value={value.stage} onChange={e=>onChange({...value,stage:e.target.value})} className={select}><option value="">All stages</option>{Object.entries(functionStages).map(([id,label])=><option key={id} value={id}>{label}</option>)}</select><select aria-label="Filter by visualization" value={value.visualization} onChange={e=>onChange({...value,visualization:e.target.value})} className={select}><option value="">All visualizations</option>{visualizations.map(x=><option key={x}>{x}</option>)}</select>{(value.file||value.stage||value.visualization||value.category!=="All")&&<button onClick={()=>onChange({category:"All",file:"",stage:"",visualization:""})} className="px-2 text-xs font-semibold text-cyan-700 hover:text-cyan-900">Clear filters</button>}</div></div>}
