import type { FunctionSpec } from "@/types";import { FunctionCard } from "./FunctionCard";
export function FunctionGalleryGrid({items,selectedId,onSelect}:{items:FunctionSpec[];selectedId:string;onSelect:(fn:FunctionSpec)=>void}){if(!items.length)return <div className="rounded-lg border border-dashed border-slate-300 bg-white p-10 text-center"><p className="font-semibold text-slate-800">No functions match</p><p className="mt-1 text-sm text-slate-500">Try a broader search or clear one of the filters.</p></div>;return <div className="grid gap-3 sm:grid-cols-2 2xl:grid-cols-3">{items.map(fn=><FunctionCard key={fn.id} fn={fn} selected={fn.id===selectedId} onSelect={()=>onSelect(fn)}/>)}</div>}
