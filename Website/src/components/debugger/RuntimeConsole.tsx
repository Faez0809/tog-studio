import { Terminal } from "lucide-react";
import type { TraceEvent } from "@/types";
export function RuntimeConsole({trace,current}:{trace:TraceEvent[];current:number}) { const lines=trace.slice(0,current+1).flatMap(s=>s.logs.map(log=>({log,time:s.timestamp}))); return <section className="overflow-hidden rounded-xl border border-slate-800 bg-slate-950 text-slate-300"><div className="flex items-center justify-between border-b border-slate-800 px-4 py-3"><h2 className="flex items-center gap-2 text-sm font-bold text-slate-100"><Terminal size={16}/> Runtime console</h2><span className="h-2 w-2 animate-pulse rounded-full bg-emerald-400"/></div><div className="h-44 overflow-auto p-4 font-mono text-xs leading-6" aria-live="polite">{lines.map(({log,time},i)=><div key={`${log}-${i}`}><span className="mr-3 text-slate-600">{(time/1000).toFixed(2).padStart(5,"0")}s</span><span className={log.includes("SUCCESS")?"text-emerald-400":log.includes("INFO")?"text-cyan-300":"text-slate-300"}>{log}</span></div>)}<span className="inline-block h-4 w-1.5 animate-pulse bg-slate-400 align-middle"/></div></section> }
