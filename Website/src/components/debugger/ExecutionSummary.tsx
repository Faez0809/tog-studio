import { CheckCircle2, Clock3, GitBranch, Target } from "lucide-react";
import type { TraceEvent } from "@/types";
import { sampleRun } from "@/data";
export function ExecutionSummary({trace,complete}:{trace:TraceEvent[];complete:boolean}) { const metrics=[{Icon:Target,label:"Stages executed",value:complete?trace.length:"—"},{Icon:GitBranch,label:"Functions called",value:complete?new Set(trace.map(x=>x.functionName)).size:"—"},{Icon:Clock3,label:"Runtime",value:complete?`${(trace.at(-1)!.timestamp/1000).toFixed(2)}s`:"—"},{Icon:CheckCircle2,label:"Confidence",value:complete?`${Math.round(sampleRun.confidence*100)}%`:"—"}]; return <section className={`rounded-xl border p-5 transition ${complete?"border-emerald-200 bg-emerald-50/60":"border-slate-200 bg-white"}`}><div className="flex items-center gap-3"><span className={`grid h-10 w-10 place-items-center rounded-full ${complete?"bg-emerald-500 text-white":"bg-slate-100 text-slate-400"}`}><CheckCircle2 size={21}/></span><div><h2 className="text-base font-bold text-slate-900">Execution summary</h2><p className="text-xs text-slate-500">{complete?"All pipeline stages completed successfully.":"Complete the trace to reveal the final result."}</p></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({Icon,label,value})=><div className="rounded-lg border border-slate-200 bg-white p-3" key={label}><p className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-wider text-slate-400"><Icon size={14}/>{label}</p><p className="mt-1 text-lg font-bold text-slate-900">{value}</p></div>)}</div>{complete&&<><div className="mt-3 rounded-lg border border-emerald-200 bg-white p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-emerald-700">Final answer</p><p className="mt-1 text-lg font-bold text-slate-950">{sampleRun.finalAnswer}</p></div><div className="mt-3 flex flex-wrap items-center gap-1.5 text-xs">{trace.map((s,i)=><span className="contents" key={s.id}><span className="rounded bg-slate-100 px-2 py-1 text-slate-600">{s.stage}</span>{i<trace.length-1&&<span className="text-slate-300">→</span>}</span>)}</div></>}</section> }
