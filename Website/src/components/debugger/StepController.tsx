import { Pause, Play, RotateCcw, SkipBack, SkipForward } from "lucide-react";

export function StepController({ current, total, playing, speed, onPrevious, onNext, onToggle, onReset, onSpeed }: { current:number; total:number; playing:boolean; speed:number; onPrevious:()=>void; onNext:()=>void; onToggle:()=>void; onReset:()=>void; onSpeed:(value:number)=>void }) {
  const button = "inline-flex items-center justify-center gap-2 rounded-md border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-700 shadow-sm transition hover:border-blue-300 hover:text-blue-700 disabled:cursor-not-allowed disabled:opacity-40";
  return <div className="flex flex-wrap items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 p-3"><button className={button} onClick={onPrevious} disabled={current===0}><SkipBack size={16}/> <span className="hidden sm:inline">Previous</span></button><button className={`${button} border-blue-600 bg-blue-600 text-white hover:bg-blue-700 hover:text-white`} onClick={onToggle}>{playing?<Pause size={16}/>:<Play size={16}/>} {playing?"Pause":"Play"}</button><button className={button} onClick={onNext} disabled={current===total-1}><SkipForward size={16}/> <span className="hidden sm:inline">Next</span></button><button className={button} onClick={onReset}><RotateCcw size={16}/> <span className="hidden sm:inline">Reset</span></button><label className="ml-auto flex items-center gap-2 text-xs font-semibold text-slate-500">Speed<select value={speed} onChange={e=>onSpeed(Number(e.target.value))} className="rounded-md border border-slate-200 bg-white px-2 py-2 text-sm text-slate-800"><option value={1400}>0.5×</option><option value={850}>1×</option><option value={450}>2×</option></select></label></div>;
}
